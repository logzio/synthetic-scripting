import React, { FunctionComponent } from 'react';

import styled from 'styled-components';

const EnvVariable: FunctionComponent = () => {
    return <button></button>;
};

export default EnvVariable;
